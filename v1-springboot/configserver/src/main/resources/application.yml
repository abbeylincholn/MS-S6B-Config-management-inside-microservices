spring:
  application:
    name: "configserver"
  profiles:
    #active: native
    active: git
  cloud:
    config:
      server:
        #native:
          #search-locations: "classpath:/config/"
          #search-locations: "file:///C:/Users/Engr. Abbey/SPRINGBOOT/microservices/config/"
        git:
          uri: "https://github.com/eazybytes/eazybytes-config.git"
          default-Label: main
          timeout: 5
          clone-on-start: true
          force-pull: true


server:
  port: 8071